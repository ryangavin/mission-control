{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for Mission Control",
  "windows": ["*"],
  "permissions": [
    "shell:allow-spawn",
    "shell:allow-kill",
    "shell:allow-open"
  ]
}
