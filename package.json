{
  "name": "mission-control",
  "private": true,
  "version": "0.0.6",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n vite,tauri -c cyan,magenta \"vite\" \"cd desktop && tauri dev\"",
    "dev:standalone": "concurrently -k -n vite,bridge -c cyan,green \"vite\" \"bun run server/standalone.ts\"",
    "build": "vite build && bun run build:bridge && cd desktop && tauri build",
    "build:vite": "vite build",
    "build:bridge": "bun build --compile --outfile=desktop/dist-bridge/mission-control-bridge ./server/standalone.ts",
    "build:bridge:mac-arm": "bun build --compile --target=bun-darwin-arm64 --outfile=desktop/dist-bridge/mission-control-bridge-aarch64-apple-darwin ./server/standalone.ts",
    "build:bridge:mac-x64": "bun build --compile --target=bun-darwin-x64 --outfile=desktop/dist-bridge/mission-control-bridge-x86_64-apple-darwin ./server/standalone.ts",
    "build:bridge:win": "bun build --compile --target=bun-windows-x64 --outfile=desktop/dist-bridge/mission-control-bridge-x86_64-pc-windows-msvc.exe ./server/standalone.ts",
    "preview": "vite preview",
    "check": "svelte-check && tsc",
    "tauri": "tauri",
    "release": "bun scripts/release.ts"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.1.0",
    "osc-js": "^2.4.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "@tauri-apps/cli": "^2.9.6",
    "@tsconfig/svelte": "^5.0.6",
    "@types/bun": "latest",
    "@types/node": "^24.10.1",
    "@types/ws": "^8.18.0",
    "concurrently": "^9.2.1",
    "svelte": "^5.43.8",
    "svelte-check": "^4.3.4",
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  }
}
